name: build-image

on:
  release:
    types:
      - created

jobs:
  build:
    uses: naari3/workflows/.github/workflows/build-multiarch-images-ghcr.yaml@main
    with:
      tag_name: ${{ github.event.release.tag_name }}1
      target_arch: linux/amd64,linux/arm64
      token: ${{ secrets.PERSONAL_ACCESS_TOKEN }}
